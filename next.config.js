/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "1.gravatar.com", "wp.rohanpoudel.com.np", "secure.gravatar.com"]
  }
}

module.exports = nextConfig
